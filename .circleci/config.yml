version: 2
jobs:
  build:
    docker:
      - image: blacklabelops/virtualbox
    working_directory: ~/alpine
    steps:
      - checkout
      - run: uname -a
      - run: packer build template.json
